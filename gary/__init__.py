"""gary: automated finance-content platform.

This package is the starter scaffold for the agents and dashboard described in
the project issues (YouTube finance transcripts, market/crypto trend scraping,
video/thumbnail generation, upload management, and a tracking dashboard).

The current implementation provides deterministic, dependency-free logic so the
app runs and is testable out of the box. Real external integrations (LLMs,
market data APIs, YouTube Data API, etc.) plug into the clearly marked seams in
the ``gary.agents`` modules.
"""

__version__ = "0.1.0"
